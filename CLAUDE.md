# Hexo CMS Local Claude Instructions

Read `docs/ai/project-context.md` for the full project context.

## Coding Preference

- `es-toolkit` is available in this workspace.
- Prefer `es-toolkit` over hand-written general-purpose helpers when built-in JavaScript APIs are not enough.
- Before adding a new utility function, check whether `es-toolkit` already covers the use case.
